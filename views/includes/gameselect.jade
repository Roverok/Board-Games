.game-select.hide
  .select-title Select Two Players
  each player in players
    .select-box(type='#{player.id}')
      .game-player(type='#{player.id}')
        .player-avatar
        .player-name #{player.name}
  .continue-button.hide Start The Game